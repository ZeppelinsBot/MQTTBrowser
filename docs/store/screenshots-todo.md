# Play Store Visual Assets — TODO Checklist (DRAFT)

**Status: NOT ready — assets below are missing and must be created before
the Play submission.**

---

## 1. Phone screenshots (min. 2 required) — ❌ MISSING

- [ ] **≥ 2 phone screenshots** (Play requires at least 2 for phones;
      recommended: 4–8)
- [ ] Needs a real device or emulator — **cannot be produced in this
      build environment** (no device/emulator here)
- Suggested scenes:
  1. Connection screen (broker list / TLS toggle)
  2. Topic tree with counter badges (v1.21 path-subscribe FAB visible)
  3. Detail screen with live/retained messages
  4. Subscribe dialog / wildcard vs. path mode
- Specs: PNG or JPEG, min. 1080 px on the short side, 16:9 or 9:16
- `TODO: capture on a real device/emulator (not doable in this build
      environment)`

## 2. Feature graphic — ❌ MISSING

- [ ] **1024 × 500 px** feature graphic (required for Play store page)
- Can be derived from the repo's `logo.png`
- `TODO: generate/adjust later (can be produced from logo.png on request)`

## 3. Hi-res icon — ❌ MISSING in store assets

- [ ] **512 × 512 px**, 32-bit PNG, no transparency in corners
- Source available: repo root `logo.png`
- `TODO: generate 512×512 from logo.png on request`

## 4. Privacy policy URL — ❌ NOT PUBLISHED YET

- [ ] `PRIVACY_POLICY.md` exists at repo root (English) — but Play needs
      it as a **public URL**
- Plan: enable **GitHub Pages** from the repo root once the branch is
      merged/published (do NOT enable now — merge/release is a later step)
- `REPLACE: privacy policy URL` (placeholder until GitHub Pages is on)

## 5. Optional / nice-to-have

- [ ] Promo video (optional)
- [ ] Feature graphic variants for tablet (optional)

---
*Nothing on this list is done yet — all items are open TODOs.*
